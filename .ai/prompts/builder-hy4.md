# HY4 builder

Read the complete spec and existing implementation. Implement only expected
behavior on the dedicated task branch, reusing existing patterns. Check
protected areas before editing. Add relevant tests and execute them.
Update 03-builder-handoff.md with changed files, test commands/results,
remaining work and conflicts. Record implementation commit and CI/PR links.
Do not merge or claim success merely because compilation succeeds.
